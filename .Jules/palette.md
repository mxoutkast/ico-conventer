## 2026-01-31 - Keyboard Shortcuts for Lists
**Learning:** Users expect `Delete`/`Backspace` keys to work on list selections, but Tkinter Treeview doesn't support this by default. Adding visual hints (e.g., in button text) helps discoverability.
**Action:** Always bind delete keys when implementing list/tree views and update related UI text.
