## 2024-04-15 - Add Keyboard Shortcuts for Accessibility
**Learning:** The Tkinter Treeview widget does not automatically support keyboard shortcuts like `Delete` for item removal or `Ctrl+A` for selecting all items, which are standard UX expectations for lists.
**Action:** Always verify if standard keyboard list interactions are bound in custom list components, and explicitly implement bindings for them if missing.
