## 2024-05-22 - Keyboard Support for List Items
**Learning:** In Tkinter `Treeview` widgets, users expect `<Delete>` and `<BackSpace>` keys to remove selected items, but this behavior is not default and must be explicitly bound.
**Action:** Always verify keyboard accessibility for list-based components and add explicit bindings for standard operations like deletion.
